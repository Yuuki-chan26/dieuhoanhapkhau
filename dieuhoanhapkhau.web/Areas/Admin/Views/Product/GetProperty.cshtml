@using dieuhoanhapkhau.biz.Models
@model List<ProducerProperties>
@{
    Layout = null;
}
<option value="">-- Tất cả --</option>
@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        <option value="@item.ProducerPropertyId">
            @item.PropertyName
        </option>
    }
}
